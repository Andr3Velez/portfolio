---
import ReactIcon from "../icons/ReactIcon.astro"
import TailwindIcon from "../icons/TailwindIcon.astro"
import FirebaseIcon from "../icons/FirebaseIcon.astro"
import ViteIcon from "../icons/ViteIcon.astro"

const { projectImg, projectTitle, projectDesc } = Astro.props
---

<article
  class="max-w-xl h-auto flex justify-start items-center gap-x-4 mt-6 sm:mt-4 md:mt-6"
>
  <div class="w-full h-auto flex">
    <div
      class="max-w-72 h-auto rounded-3xl border border-gray-500 p-1 overflow-hidden hover:scale-110 hover:cursor-pointer transition-transform duration-300 ease-in-out"
    >
      <img src={projectImg} alt="pos-screenshot" class="object-cover" />
    </div>
    <div class="w-full flex flex-col justify-around pl-4">
      <h3 class="text-xl font-bold text-left block">{projectTitle}</h3>
      <div class="flex gap-x-1">
        <ReactIcon class="w-7 h-7" />
        <TailwindIcon />
        <FirebaseIcon />
        <ViteIcon />
      </div>
      <p class="text-md font-medium break-words">
        {projectDesc}
      </p>
    </div>
  </div>
</article>
